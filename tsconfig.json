{
  "extends": "./.quasar/tsconfig.json",
  "compilerOptions": {
    // Глушимо попередження TS 6.0 про baseUrl
    "ignoreDeprecations": "6.0"
  },
  // Явно реєструємо файли автоімпорту, не чіпаючи масиви include/exclude
  "files": [
    "src/auto-imports.d.ts",
    "src/components.d.ts"
  ]
}